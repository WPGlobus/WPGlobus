/*
 * Copyright (c) 2020, TIV.NET INC. All Rights Reserved.
 */

const
	// Settings = require("./Settings"),
	Cart = require("./Cart"),
	Checkout = require("./Checkout")
;
/*

const DOM_PRICE = ".woocommerce-Price-amount";

const DOM = {
	product_total: `.product-total ${DOM_PRICE}`,
	cart_subtotal: `.cart-subtotal ${DOM_PRICE}`,
	tax_rate: `.tax-rate ${DOM_PRICE}`,
	shipping: `.shipping ${DOM_PRICE}`,
	order_total: `.order-total ${DOM_PRICE}`,
	checkout_button: '.checkout-button'
};
*/

module.exports = {
	// DOM_PRICE,
	// DOM,
	Cart,
	Checkout,
	// Settings
};
